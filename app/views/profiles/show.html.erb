
  <div class="profile-banner d-flex align-items-center" style="background-image: url(<%= image_path "profile-banner.jpg" %>);">
  </div>
    <div class="container text-center">
      <div class="row">
        <div class="col-sm-8 offset-sm-2 mt-5 profile-card">
          <div class="profile-photo-avatar">
            <% if current_user.avatar.file.nil? %>
              <%= image_tag "default_avatar.png", width: 150, height: 150, class: "rounded-circle" %>
            <% else %>
              <%= cl_image_tag @user.avatar , width: 150, height: 150, crop: :fill, class: "rounded-circle" %>
            <% end %>
          </div>
          <div class="profile-details-text">
            <h1><%= @user.first_name + " " + @user.last_name %></h1>
            <h5><%= @user.email %></h5>
            <p>Membre depuis: <%= @user.created_at.strftime("%d / %m / %Y") %></p>
            <%= simple_form_for @user, url: profile_path do |f| %>
              <div class="photo-upload-input-box">
                <%= f.input :avatar, input_html: { class: 'hidden', id: 'photo-input'}, label_html: { class: 'upload-photo'}, label: '📷 Télécharger une photo'%>
              </div>
              <div class="photo-preview-box">
                <%= image_tag "", width: 100, class: "hidden", id: "photo-preview" %>
              </div>
              <%= f.submit "Enregistrer", class: "btn btn-primary" %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
